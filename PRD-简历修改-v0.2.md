# 简历修改产品 PRD

> 版本：v0.2（讨论稿）  
> 日期：2026-08-30  
> 状态：需求定义中  
> 项目位置：个人作品网站第二个项目

## 1. 产品概述

### 1.1 产品名称

暂定名：简历修改

后续可根据品牌定位调整为更具产品感的名称。

### 1.2 一句话定义

面向应届生及工作 1–3 年求职者的 AI 简历优化产品。用户上传中文简历并提供目标岗位 JD 后，系统以资深 HR 的视角完成岗位解读、简历诊断、内容重写、结构重排、经历缺口分析和可导出的针对性简历。

### 1.3 核心价值

产品不只负责“润色文字”，而是帮助用户回答三个问题：

1. 这个岗位真正需要什么人？
2. 当前简历为什么无法有效证明用户适合该岗位？
3. 在不虚构事实的前提下，应该如何修改和补强？

### 1.4 产品定位

- 核心角色：可解释、懂岗位匹配、坚持真实性的 AI 资深 HR。
- 目标结果：生成一份更符合目标 JD、对 HR 和 ATS 都更友好的简历版本。
- 核心差异：JD 与简历双向解析、修改依据可追溯、缺口诊断、真实性护栏。

## 2. 背景与问题

目标用户通常面临以下问题：

- 不知道 JD 中哪些要求是硬性门槛，哪些只是加分项。
- 使用同一份简历投递不同岗位，内容缺乏针对性。
- 经历描述停留在“负责什么”，没有体现成果、贡献和能力。
- 不知道哪些内容应该删除、前置、合并或展开。
- 套用模板后版式更漂亮，但不一定适合 ATS 解析。
- 获得的建议过于笼统，例如“增加实习”“多写项目”，无法执行。
- 通用 AI 工具容易虚构数据、职责或经历，且修改原因不透明。

## 3. 产品目标与非目标

### 3.1 MVP 目标

- 让用户在 10 分钟内获得一份针对单个 JD 的完整诊断报告。
- 让用户清楚理解每项修改对应的岗位要求和修改原因。
- 在用户确认事实后，生成可编辑、可导出的中文简历。
- 识别关键经历缺口，并给出可执行的补强方向或明确标注的虚拟示例。
- 保证系统不会把未经确认的虚拟内容混入最终简历。
- 不限制用户输入的岗位类别，通过通用岗位解析框架适配不同 JD。
- 以作品集演示为第一目标，同时保证核心流程可以真实运行。

### 3.2 非目标

MVP 暂不包含：

- 自动投递职位。
- 职位搜索或招聘平台聚合。
- 完整的面试训练系统。
- 长期职业规划服务。
- 自动生成无法由用户证明的经历、项目或数据。
- 将匹配分数解释为面试或录取概率。

## 4. 目标用户

### 4.1 核心用户

应届毕业生及工作 1–3 年、正在投递中文岗位的求职者。

### 4.2 典型特征

- 缺少专业简历写作经验。
- 对目标岗位有初步方向，但不会解读 JD。
- 有一定经历素材，但不会提炼或量化。
- 可能缺少直接相关实习或项目，需要迁移已有经验。
- 希望快速得到专业反馈，同时保留对最终内容的控制权。

### 4.3 核心使用场景

- 第一次为某个目标岗位制作针对性简历。
- 已投递多个岗位但反馈较少，希望诊断问题。
- 从相邻专业或岗位转型，需要识别可迁移能力。
- 已有简历内容，但希望重排成更专业、ATS 友好的格式。

## 5. 用户主流程

1. 用户进入项目介绍页，点击“开始优化”。
2. 上传 PDF/DOCX 简历，或上传简历截图。
3. 粘贴 JD 文本、上传 JD 截图，或填写招聘链接。
4. 系统解析简历与 JD，并要求用户确认解析结果。
5. 用户补充求职背景和必要信息。
6. 系统生成岗位画像和简历诊断。
7. 用户查看总体评分、优势、风险和缺口。
8. 信息不足时，系统向用户发起一轮结构化追问。
9. 系统直接生成整份优化简历。
10. 用户在对比工作台查看原简历和优化版，并查看重点差异与修改原因。
11. 用户编辑优化版，或将不满意的段落恢复为原文。
12. 用户选择版式并预览最终简历。
13. 用户无需登录即可导出 DOCX、PDF 和修改报告。

## 6. 功能需求

### 6.1 简历输入与解析

#### P0

- 支持上传 PDF、DOCX。
- 支持上传清晰的单张或多张简历截图。
- 自动识别姓名、联系方式、教育、实习、工作、项目、技能、证书等模块。
- 保留原文、模块顺序和解析置信度。
- 解析后允许用户人工校正。
- 文件格式异常、扫描质量差或内容缺失时给出明确反馈。

#### P1

- 支持粘贴纯文本简历。
- 支持历史简历版本复用。
- 支持对个人敏感信息进行预览隐藏。

### 6.2 JD 输入与岗位画像

#### P0

- 支持粘贴 JD 文本。
- 支持上传 JD 截图并识别文字。
- 支持输入公开招聘链接并提取页面中的岗位信息。
- 链接无法访问、需要登录或反爬限制时，引导用户粘贴文本或上传截图。
- 提取岗位名称、公司、职责、硬性要求、加分项、关键词和岗位级别。
- 区分“明确写出”和“系统推断”的岗位要求。
- 允许用户修正岗位画像。

#### P1

- 识别 JD 中的模糊表述、模板化内容和潜在矛盾。
- 支持多个 JD 的共性比较。

### 6.3 简历诊断

#### P0

诊断维度包括：

- 岗位相关性。
- 硬性条件覆盖度。
- 经历成果表达。
- 专业能力可信度。
- 内容完整性。
- 信息优先级。
- HR 快速阅读体验。
- ATS 兼容性。
- 可能引发面试追问或质疑的内容。

每个维度必须包含：数字分数、等级、证据、问题说明和行动建议。

MVP 建议使用 100 分制，并映射为四个等级：

- 85–100：高度匹配。
- 70–84：匹配良好。
- 50–69：需要加强。
- 0–49：存在明显缺口。

分数由可见的诊断维度加权组成；当简历或 JD 解析置信度不足时，应同时提示分数可信度。

总体匹配分只用于帮助用户理解内容差距，不表示投递成功率或录取概率。

### 6.4 内容优化

#### P0

- 支持对单条经历进行重写。
- 将职责型表达调整为行动与成果导向表达。
- 支持 STAR、CAR 等结构，但不强制在页面中暴露术语。
- 识别并合并重复描述。
- 删除低价值、与目标岗位弱相关的内容。
- 将与 JD 高相关的经历或技能前置。
- 在不改变事实的前提下调整措辞、顺序和篇幅。
- 每项建议展示原文、建议版本、修改理由、对应 JD 要求和事实状态。
- 系统完成整份简历优化后，一次性生成可比较的结果。
- 用户可查看重点差异、编辑优化版、恢复单个段落或撤销操作。

#### P1

- 提供“保守修改”和“强化表达”两种优化强度。
- 根据产品、运营、设计、技术等岗位切换表达侧重点。
- 支持批量接受低风险修改。

### 6.5 信息追问

#### P0

系统遇到以下情况时应优先追问，而不是直接生成：

- 工作成果缺少数据或范围。
- 团队成果与个人贡献无法区分。
- 项目角色、对象、周期或方法不清楚。
- 某项技能只被列出，但没有经历证据。
- 时间线存在空白、冲突或异常。

追问应尽量提供易回答的选项、示例和“不确定/没有数据”入口。

### 6.6 缺口分析与虚拟示例

#### P0

系统将缺口划分为：

1. 用户可能做过但没有写出的内容。
2. 用户已有相邻经历，可以迁移或重新表达的内容。
3. 用户确实缺少、需要未来补强的经历或能力。

用户选择后，系统可以生成虚拟项目或经历示例，但必须满足：

- 页面持续显示“示例内容，不代表你的真实经历”。
- 示例使用独立视觉样式，不与正式简历内容混淆。
- 默认不能直接写入最终简历。
- 用户必须将示例替换为真实信息，或逐项确认事实后才能转为正式内容。
- 虚拟数据使用明显占位符，如“提升 XX%”，不得生成看似真实的随机数据。
- 导出前再次进行事实确认检查。

系统还应给出真实补强方案，例如项目方向、建议产出物、可验证证据和预计优先级。

### 6.7 结构重排与版式

#### P0

- 根据用户背景和 JD 推荐模块顺序。
- 提供至少一套单栏、ATS 友好的中文简历模板。
- 控制简历在合理页数内，并提示信息密度问题。
- 避免依赖图标、复杂表格、多栏布局表达关键信息。
- 支持预览和基础样式调整。

#### P1

- 增加互联网、咨询、金融等不同风格模板。
- 提供一页版和详细版。
- 支持模块拖拽排序。

### 6.8 导出与版本管理

#### P0

- 导出 DOCX。
- 导出 PDF。
- 导出简历诊断与修改报告。
- 保留原始版本和当前版本。
- 导出前展示真实性检查和未完成项。
- 无需注册或登录即可完成导出。

#### P1

- 在当前匿名会话中为不同岗位生成多个针对性版本。
- 支持版本命名、复制和比较。

### 6.9 岗位适配

#### P0

- 产品入口不限制岗位类别，用户可以输入任意中文 JD。
- 系统依据 JD 动态识别岗位职责、技能、行业、职级和能力要求。
- 对所有岗位使用一致的诊断骨架，同时根据 JD 动态调整维度权重。
- 当系统对专业术语、行业规则或岗位要求的置信度不足时，明确提示用户复核。
- 不宣称所有行业均已达到专家级适配；作品集案例中应区分“可使用岗位”和“已验证岗位”。

#### P1

- 建立岗位测试集，并逐步标注产品、设计、运营、技术、市场、职能等类别的验证状态。
- 根据岗位类型增加行业化表达建议和专属检查项。

## 7. 页面与信息架构

MVP 建议包含以下页面：

1. 项目介绍/落地页：价值、工作方式、隐私说明、开始入口。
2. 上传页：简历输入、JD 输入、文件状态和示例数据入口。
3. 解析确认页：确认简历结构和岗位画像。
4. 诊断总览页：匹配概览、优势、风险、缺口和优化优先级。
5. 对比工作台：左侧原简历，右侧完整优化版；高亮重点差异并展开修改原因。
6. 缺口补强页：追问、迁移建议、虚拟示例和真实行动方案。
7. 模板与预览页：结构、版式、分页和 ATS 检查。
8. 导出页：文件格式、版本名称、事实确认和删除数据入口。

## 8. AI 规则与产品护栏

### 8.1 事实状态

每条内容在系统内部应标记为：

- 原简历已有事实。
- 用户补充并确认的事实。
- 系统对已有事实的改写。
- 待用户确认的推断。
- 虚拟示例。

只有前三类内容可以进入最终导出版本。

### 8.2 可解释性

每条重要建议应能追溯到：

- 原简历证据。
- JD 对应要求。
- 使用的优化原则。
- 预期改善的诊断维度。

### 8.3 禁止行为

- 不虚构公司、学校、职位、证书、项目或工作成果。
- 不擅自生成真实感数据。
- 不将岗位推断包装成招聘方明确要求。
- 不根据性别、年龄、民族、婚育等敏感属性提出歧视性建议。
- 不承诺面试、Offer 或录取概率。

## 9. 数据与隐私

### 9.1 MVP 原则

- 上传前明确说明处理目的和数据保存方式。
- 传输和存储过程加密。
- 默认采用最短可行保存周期。
- 用户可以主动删除简历、JD、生成版本和账号数据。
- 未经明确授权，不使用用户简历训练模型。
- 日志和分析数据尽量去标识化。
- 导出文件不得包含隐藏提示词、内部评分或调试信息。
- 用户无需创建账号，系统仅生成匿名会话标识。
- MVP 不提供跨设备历史记录或长期账号资料库。

### 9.2 待确认事项

- 匿名用户数据是在导出后立即删除，还是最多保留 24 小时。
- 是否需要在服务端保存原始文件。

## 10. 成功指标

### 10.1 核心指标

- 完成一次“上传—诊断—导出”的用户比例。
- 用户保留、编辑或恢复优化段落的比例。
- 从进入上传页到首次获得诊断的完成率。
- 生成结果的事实确认通过率。

### 10.2 质量指标

- HR 盲评中，优化版优于原版的比例。
- JD 关键要求识别准确率。
- 简历字段解析准确率。
- 未经确认的虚构内容进入导出文件的比例，目标为 0。
- DOCX/PDF 导出成功率。
- ATS 基础解析通过率。

### 10.3 体验指标

- 首次诊断等待时间。
- 用户完成必要追问的比例。
- 用户对修改理由“有帮助”的评价。
- 用户主动删除数据的操作成功率。

## 11. MVP 验收标准

- 用户可通过 PDF、DOCX 或截图上传中文简历。
- 用户可通过文本、截图或公开链接提供 JD。
- 系统可展示并允许修正结构化解析结果。
- 系统可生成至少 8 个诊断维度的解释性报告。
- 每条内容修改都可追溯到原文和 JD。
- 系统可直接生成整份优化简历，并支持原版与优化版对比。
- 用户可以编辑优化版、恢复单个段落和撤销操作。
- 系统可以发起信息追问，并允许用户回答“不确定”。
- 所有虚拟示例都有持续可见的标识。
- 未确认的推断或虚拟示例无法进入最终导出文件。
- 系统可导出 DOCX、PDF 和修改报告。
- 用户无需登录或付费即可完成 MVP 全流程。
- 用户可以删除原始文件和生成内容。
- 使用预设测试集验证简历解析、JD 解析、事实护栏和导出结果。

## 12. 风险与应对

### 12.1 招聘链接无法稳定解析

应对：链接解析失败后保留文本粘贴和截图上传作为稳定降级路径。

### 12.2 AI 生成内容不真实

应对：事实状态标记、字段级确认、虚拟示例隔离、导出前事实检查。

### 12.3 匹配评分给用户错误信心

应对：展示评分依据、置信度和具体证据，不使用“录取率”表述。

### 12.4 简历优化趋同

应对：保留个人表达、基于真实经历追问、避免套用单一措辞模板。

### 12.5 输出版式与 ATS 冲突

应对：MVP 优先单栏和标准标题；将装饰型模板放在后续版本。

### 12.6 作品集项目范围过大

应对：先用一个完整闭环和一个代表性案例证明产品价值，再扩展模板、多岗位和英文能力。

## 13. 产品路线图

### 阶段 1：作品集 MVP

- 完成核心用户流程和关键页面。
- 使用一套 ATS 友好模板。
- 准备一份示例简历与 JD，形成完整演示。
- 实现可解释诊断、整份生成与差异比较、缺口分析和导出。
- 对任意中文 JD 开放输入，并通过示例测试集验证主要岗位类别。
- 所有 MVP 功能免费开放，不接入支付系统。

### 阶段 2：体验增强

- 多岗位版本管理。
- 多种优化强度。
- 多种行业模板。
- 更丰富的追问和补强计划。

### 阶段 3：能力扩展

- 英文和双语简历。
- 多 JD 对比。
- 历史版本效果分析。
- 与面试准备或求职行动计划衔接。

## 14. 作品集呈现建议

个人作品网站的项目详情页建议展示：

1. 问题：通用 AI 简历润色缺少岗位理解、解释和事实保护。
2. 洞察：用户需要的不是一次性重写，而是可确认的决策过程。
3. 产品原则：岗位针对性、可解释、真实性、用户控制。
4. 核心流程：上传、解析、诊断、修改、补强、导出。
5. 重点界面：诊断总览、整份简历对比工作台、缺口补强、事实检查。
6. AI 设计：事实状态、追问策略和虚拟示例隔离。
7. 验证方式：HR 盲评、解析测试、护栏测试和可用性测试。
8. 反思：产品如何平衡表达强化、真实性和用户焦虑。

## 15. 待确认决策

下一轮优先确认：

1. 匿名会话中的文件和生成结果何时自动删除。
2. 生成完整优化版前，系统最多向用户追问多少个问题。
3. 原版与优化版采用并排、切换还是差异文档的比较方式。
4. 分数的各维度权重是否完全展示给用户。
5. MVP 提供一套还是多套简历模板。
6. 作品集演示案例使用真实匿名简历还是虚构测试人物。
